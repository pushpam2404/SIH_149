# GUI
PySide6>=6.7,<7

# device enumeration
psutil>=5.9

# reporting
reportlab>=4.0

# verification math (entropy/chi-square)
numpy>=1.26

# recovery: filesystem-aware carving (Sleuth Kit bindings)
# Requires `sleuthkit` installed via the system package manager BEFORE this,
# e.g. `brew install sleuthkit` (macOS) or `apt-get install sleuthkit` (Linux).
# See scripts/setup_env.sh. Optional — the app degrades gracefully if missing.
pytsk3>=20230125; python_version < "3.13"

# recovery: secondary file-type cross-check signal
# Requires `libmagic` installed via the system package manager first
# (`brew install libmagic` / `apt-get install libmagic1 libmagic-dev`).
# Optional — the app degrades gracefully if missing.
python-magic>=0.4.27

# recovery: deep-learning file-type classification (Google Magika)
magika>=0.5.0

# recovery: fuzzy hashing (ssdeep / Context Triggered Piecewise Hashing)
ppdeep>=20260221

# testing
pytest>=8.0
pytest-qt>=4.4

